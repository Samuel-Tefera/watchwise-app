import React from 'react';
import MovieGrid from '../features/Movie/MovieGrid';
import MoviesList from '../features/Movie/MoviesList';

export default function Movies() {
  return (
    <div>
      <MoviesList />
    </div>
  );
}
